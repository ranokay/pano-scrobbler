name: Pano Scrobbler macOS

on:
  workflow_dispatch:
  push:
    paths:
      - "apple/**"
      - "version.txt"
      - "changelog.md"
      - ".github/workflows/apple-macos.yml"
  pull_request:
    paths:
      - "apple/**"
      - "version.txt"
      - "changelog.md"
      - ".github/workflows/apple-macos.yml"

permissions:
  contents: read

jobs:
  macos:
    name: Test and build macOS (${{ matrix.release_arch }})
    runs-on: macos-26
    strategy:
      fail-fast: false
      matrix:
        include:
          - release_arch: arm64
            swift_arch: arm64
            dmg_name: pano-scrobbler-macos-arm64.dmg
          - release_arch: x64
            swift_arch: x86_64
            dmg_name: pano-scrobbler-macos-x64.dmg
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Show toolchain
        working-directory: apple
        run: |
          swift --version
          xcodebuild -version

      - name: Import Developer ID certificate
        env:
          MACOS_CERTIFICATE_P12_BASE64: ${{ secrets.MACOS_CERTIFICATE_P12_BASE64 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ github.run_id }}
        run: |
          if [ -z "$MACOS_CERTIFICATE_P12_BASE64" ]; then
            echo "No Developer ID certificate configured; local validation will use ad-hoc signing."
            exit 0
          fi

          certificate_path="$RUNNER_TEMP/developer-id.p12"
          keychain_path="$RUNNER_TEMP/app-signing.keychain-db"

          echo "$MACOS_CERTIFICATE_P12_BASE64" | base64 --decode > "$certificate_path"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security import "$certificate_path" -P "$MACOS_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$keychain_path"
          security list-keychain -d user -s "$keychain_path"
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$keychain_path"
          security find-identity -v -p codesigning "$keychain_path"

      - name: Build
        working-directory: apple
        run: swift build

      - name: Test
        working-directory: apple
        run: swift test

      - name: Build DMG
        env:
          MACOS_CODESIGN_IDENTITY: ${{ secrets.MACOS_CODESIGN_IDENTITY }}
          MACOS_DMG_CODESIGN_IDENTITY: ${{ secrets.MACOS_DMG_CODESIGN_IDENTITY || secrets.MACOS_CODESIGN_IDENTITY }}
          PANO_DISCORD_CLIENT_ID: ${{ secrets.PANO_DISCORD_CLIENT_ID }}
          ARCHS: ${{ matrix.swift_arch }}
          DMG_NAME: ${{ matrix.dmg_name }}
        run: bash apple/scripts/build_dmg.sh

      - name: Upload macOS DMG artifact
        uses: actions/upload-artifact@v6
        with:
          name: pano-scrobbler-macos-${{ matrix.release_arch }}
          path: dist/${{ matrix.dmg_name }}

  publish-release:
    name: Publish macOS release
    needs: [macos]
    if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download macOS DMG artifact
        uses: actions/download-artifact@v8
        with:
          pattern: pano-scrobbler-macos-*
          path: dist
          merge-multiple: true

      - name: Read release metadata
        id: release_metadata
        shell: bash
        run: |
          verCode="$(tr -cd '0-9' < version.txt)"
          if [ -z "$verCode" ]; then
            echo "version.txt does not contain a numeric version code" >&2
            exit 1
          fi
          verName="$(printf '%d.%02d' "$((10#$verCode / 100))" "$((10#$verCode % 100))")"
          echo "verCode=$verCode" >> "$GITHUB_OUTPUT"
          echo "verName=$verName" >> "$GITHUB_OUTPUT"

      - name: Create macOS GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: macos-${{ steps.release_metadata.outputs.verCode }}
          target_commitish: ${{ github.sha }}
          name: ${{ steps.release_metadata.outputs.verName }} macOS
          body_path: changelog.md
          files: dist/pano-scrobbler-macos-*.dmg
          make_latest: false
          overwrite_files: true
          fail_on_unmatched_files: true
